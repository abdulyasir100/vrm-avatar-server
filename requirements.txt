# The real dependency list lives in requirements-docker.txt, because that is
# the only file the Dockerfile copies before `pip install` (it is COPYed on its
# own so the layer caches). Including it here keeps local installs identical to
# production — the two lists had drifted, and this one was missing eight
# packages the app imports.
-r requirements-docker.txt

# Optional: RVC voice conversion. Off unless RVC_ENABLED=true, and nothing else
# needs torch, so this is deliberately not installed by default — the CUDA
# wheels are ~2.5 GB and do not exist on PyPI at all:
#
#   pip install torch==2.5.1+cu121 torchaudio==2.5.1+cu121 rvc-python==0.1.5 \
#     --index-url https://download.pytorch.org/whl/cu121
